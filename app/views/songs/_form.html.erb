<%= form_for [@var, @song] do |f| %>
  <%= f.label :name%>
  <%= f.text_field :name%>

  <% if !params.key?("artist_id") %>
    <%= f.label :artist_id%>
    <%= f.text_field :artist_id%>
  <% end %>

  <%= f.submit %>

<%end%>